<%= form_for(@phone) do |f| %>
  <% if @phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% @phone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ptid %><br />
    <%= f.text_field :ptid %>
  </div>
  <div class="field">
    <%= f.label :p_number %><br />
    <%= f.text_field :p_number %>
  </div>
  <div class="field">
    <%= f.label :p_type %><br />
    <%= f.text_field :p_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
